---
import BaseLayout from '@/layouts/BaseLayout.astro'
import PageHero from '@/components/sections/PageHero.astro'
import { t } from '@/lib/i18n'
const lang = (Astro.currentLocale ?? 'ru') as 'ru' | 'en' | 'de'
---
<BaseLayout title="Terms & Conditions — Krüger Motor Oil">
  <PageHero
    title={t(lang, 'legal.termsTitle')}
    breadcrumbs={[{ label: t(lang, 'common.breadcrumbHome'), href: '/' }, { label: 'Terms' }]}
  />
  <section class="bg-bg py-12 md:py-16">
    <div class="container-page max-w-3xl text-text-muted leading-relaxed space-y-4">
      <p class="text-sm text-text-muted bg-bg-soft border border-border-soft rounded-md px-4 py-3">{t(lang, 'legal.draftNotice')}</p>
      <p>Условия использования сайта Krüger Motor Oil — рабочий шаблон.</p>
    </div>
  </section>
</BaseLayout>
